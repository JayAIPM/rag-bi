# RAG-Backend 开发计划

## 项目概述

本项目为企业内部知识 RAG 系统的后端服务部分，基于 Node.js 生态开发，支持本地私有化部署，开箱即用，兼顾 POC 演示与生产级落地需求。

## 核心技术栈

- **后端**：Node.js + Express.js
- **数据库**：MongoDB + LanceDB
- **RAG**：Ollama（本地 LLM + 嵌入模型）
- **检索**：混合检索（向量 + BM25）

---

## 当前进度

### 已完成 ✅

| 模块 | 说明 |
|------|------|
| 项目初始化 | Express 服务、数据库连接、基础架构 |
| 认证授权 | JWT 登录、Token 管理、权限验证 |
| 知识库管理 | CRUD 完整实现 |
| 文档处理 | PDF/Word/TXT/MD/CSV 解析、分块、向量化 |
| 检索引擎 | 混合检索（RRF 融合） |
| 对话管理 | 基础对话、流式响应、历史管理 |
| 引用溯源 | 引用提取、documentName 修复 |
| 多轮对话 | 完整支持 |
| 健壮性增强 | 超时保护、流式中断处理、最大长度限制 |

---

## 待完成模块

### 4. 文档处理模块

#### 4.2.3.1 语义结构解析优化 ⏳

**阶段一：提升文档解析能力**
- [x] ~~替换 pdf-parse → 使用 pdfjs-dist 提取带结构的文本~~ ⏭️ 暂跳过
- [x] 替换 mammoth.extractRawText() → 使用 convertToHtml 解析结构 ✅
- [x] 为解析结果添加结构元数据 ✅

**阶段二：实现父子分层分块** ✅
- [x] 实现语义单元识别（章节、主题块、段落组）
- [x] 构建文档层级关系树（hierarchyPath）
- [x] 父分块保持完整上下文（parentContext）
- [x] 子分块提供精细内容（SentenceSplitter 细切）

**阶段三：上下文重建机制** ✅
- [x] 在检索时，根据子分块引用重建完整父上下文 ✅
- [x] 优化 LLM 的 Prompt 包含层级信息（`【章节】xxx\n【内容】xxx`）✅
- [x] 验证点：复杂文档的问答质量提升，上下文逻辑连贯性增强 ✅

#### 4.2.4 向量化处理 ⏳
- [x] 直接调用 Ollama API 进行文本向量化
- [ ] 处理大文档的分批向量化（可后续实现）

#### 4.2.5 向量存储到 LanceDB ⏳
- [x] 初始化 LanceDB 连接、存储向量数据
- [ ] 建立索引优化检索性能

#### 4.2.6 重试机制 ⏳
- [ ] 对可重试错误实现 2-3 次重试
- [ ] 设置重试间隔（2s/5s 递增）
- [ ] 更新处理状态和错误信息

#### 4.4 日志集成 ⏳
- [ ] 记录文件上传、解析、分块、向量化各阶段的耗时和状态
- [ ] 记录错误信息和重试情况

---

### 5. 检索引擎模块

#### 5.4 批次4：权限过滤 ✅

**任务**：实现权限前置过滤（验证用户知识库访问权限）✅

**实现内容**：

**阶段一：创建权限验证服务** ✅
- [x] 新建 `src/services/permissionService.js`
- [x] 实现 `checkKnowledgeBaseAccess(userId, knowledgeBaseId)` 函数
  - [x] 检查用户是否为知识库所有者（KnowledgeBase.owner）
  - [x] 检查用户是否为管理员（拥有所有知识库访问权限）
  - [x] 抛出 `AppError` 权限不足错误

**阶段二：集成到对话服务** ✅
- [x] 修改 `chatService.buildContext()`
- [x] 在调用 `retrievalService.hybridSearch()` 前验证权限
- [x] 覆盖流式/非流式两种请求方式

**阶段三：更新 Controller 层** ✅
- [x] 修改 `chatController.ask()` 传递 userRole
- [x] 修改 `chatController.askStream()` 传递 userRole

**验证方式**：
1. 用户A创建知识库KB_A，上传文档
2. 用户B尝试查询KB_A → 应返回403权限错误
3. 用户A查询KB_A → 正常返回结果
4. 管理员查询任意知识库 → 正常返回结果

#### 5.5 批次5：智能查询重写 ✅

**任务**：实现智能查询重写（使用 deepseek-r1:8b 模型）

**实现内容**：

**阶段一：配置模型参数** ✅
- [x] 在 `src/config/llm.js` 中添加查询重写专用模型配置
- [x] 新增 `QUERY_REWRITE_CONFIG` 配置项，指定使用 `deepseek-r1:8b`
- [x] 配置重写超时时间和最大 token 限制

**阶段二：设计查询重写 Prompt** ✅
- [x] 设计查询重写的 System Prompt
- [x] 明确重写规则：保持原意、简洁表达、提取关键词

**阶段三：创建查询重写服务** ✅
- [x] 新建 `src/services/queryRewriteService.js`
- [x] 实现 `rewriteQuery(originalQuery)` 函数
- [x] 调用 Ollama API 的 `/api/generate` 端点
- [x] 实现降级策略（重写失败时使用原始查询）

**阶段四：集成到检索流程** ✅
- [x] 修改 `retrievalService.hybridSearch()`
- [x] 在向量检索前调用 `queryRewriteService.rewriteQuery()`
- [x] 将重写后的查询用于后续检索

**阶段五：配置管理** ✅
- [x] 添加环境变量支持
- [x] 添加开关控制（`QUERY_REWRITE_ENABLED`）

**使用方式**：
1. 设置环境变量 `QUERY_REWRITE_ENABLED=true` 启用查询重写
2. 确保已部署 `deepseek-r1:8b` 模型
3. 默认关闭，保留原始检索逻辑

#### 5.6 批次6：重排序 ⏳

**任务**：实现重排序功能（BGE-Reranker-v2-m3 通过 Ollama 本地部署）

**⚠️ 重要说明**：
- 目前 Ollama 不支持 `/api/rerank` 端点
- 此功能已实现，但安全禁用（通过 `RERANK_FEATURE_AVAILABLE` 标记）
- 需要找到其他方案或使用其他重排序服务

**已实现**：
- [x] 配置模型参数（`src/config/llm.js`）
- [x] 创建重排序服务（`src/services/rerankService.js`）
- [x] 集成到检索流程（`retrievalService.js`）
- [x] 完善的错误处理和降级机制
- [x] 详细的日志输出

**当前状态**：
- 默认安全禁用（`RERANK_FEATURE_AVAILABLE = false`）
- 可以正常编译运行，不会影响其他功能
- 等待支持重排序的方案出现

**使用方式（备用）**：
1. 修复 `RERANK_FEATURE_AVAILABLE = true`
2. 设置环境变量 `RERANK_ENABLED=true` 启用重排序
3. 确保已部署重排序模型


---

### 6. 对话管理模块

#### 6.3 批次3：引用溯源与幻觉抑制 ⏳

**阶段一：完善引用数据结构** ✅
- [x] 修复 `extractReferences` 函数
- [x] 补充引用来源元数据

**阶段二：Prompt 优化与幻觉抑制** ✅
- [x] 优化 System Prompt，强化引用约束
- [x] 修复 documentName 字段缺失问题
- [ ] 检索结果冲突处理规则：（暂不具备条件）
  - 需要 chunks 数据包含 timestamp、hierarchyLevel 等元数据字段

**阶段三：引用校验机制（可选）**
- [ ] 在保存回答前验证引用序号与参考信息的对应关系
- [ ] 验证引用内容与原始文档的一致性

#### 6.4 实现多轮对话支持 ✅
- [x] 批次1：接口参数调整（新增 chatId）
- [x] 批次2：历史对话加载逻辑
- [x] 批次3：历史对话作为 LLM 上下文
- [x] 批次4：对话记录更新而非新建

#### 6.5 实现 Query 预处理和拒答逻辑（后续迭代） ⏳
- [ ] 实现 Query 预处理：拼写纠错、意图识别等
- [ ] 实现拒答逻辑：识别无关问题直接返回 "暂无相关信息"

---

### 7. 用户管理模块 ⏳

- [ ] 实现用户列表接口
- [ ] 实现用户创建接口
- [ ] 实现用户更新接口
- [ ] 实现用户删除接口
- [ ] 实现角色管理接口
- [ ] 实现权限分配接口

---

### 8. 统计分析模块 ⏳

- [ ] 实现运营仪表盘核心指标接口
- [ ] 实现文档统计信息接口
- [ ] 实现对话统计信息接口
- [ ] 实现性能监控

---

### 9. 安全与稳定性 ⏳

- [ ] 实现输入输出内容安全过滤
- [ ] 实现错误处理机制
- [ ] 实现故障容错（LLM 调用重试、文档解析容错）
- [ ] 实现服务降级策略
- [ ] 实现健康检查机制
- [ ] 实现日志记录

---

## 建议开发优先级

| 优先级 | 模块 | 说明 |
|--------|------|------|
| **P0** | 5.4 权限过滤 | 安全是底线，当前检索存在权限漏洞 |
| **P1** | 7. 用户管理 | 企业级应用基础，MVP 演示必备 |
| **P2** | 4.2.3.1 阶段三上下文重建 | 继续语义结构解析优化 |
| **P3** | 6.5 Query预处理 / 6.3阶段三引用校验 | 可选增强 |

---

本计划将作为项目开发的指导文档，确保开发过程有序进行，按时完成 MVP 版本的开发。
